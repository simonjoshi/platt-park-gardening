services:
  - type: web
    name: deal-hunter-game
    runtime: node
    buildCommand: "npm install && npm run build"
    startCommand: "npm run preview"
    envVars:
      - key: NODE_VERSION
        value: 20
    routes:
      - type: rewrite
        source: "/*"
        destination: /index.html
